# Copy this file to secrets.toml and fill in your password.
# secrets.toml is gitignored — never commit actual passwords.
#
# To disable the password gate, leave APP_PASSWORD empty or remove the line.

APP_PASSWORD = "your-password-here"
